pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "sticker2d"
include ':app'

include ':libCameraX'
project(':libCameraX').projectDir = new File(settingsDir, '../BazookaLibs/libCameraX')
